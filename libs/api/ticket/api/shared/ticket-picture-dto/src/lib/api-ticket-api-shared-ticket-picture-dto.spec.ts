import { TicketPictureDto } from './api-ticket-api-shared-ticket-picture-dto';

describe('apiTicketApiSharedTicketPictureDto', () => {
  it('should work', () => {
    expect(new TicketPictureDto()).toBeDefined(
    );
  });
});
